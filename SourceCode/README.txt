These three packages should be install:

pip install scikit-learn
pip install matplotlib
pip install numpy


When running main.py, 2 arguments must be given, the first is 'inputfilename'.txt the second is 'outputfilename'.txt